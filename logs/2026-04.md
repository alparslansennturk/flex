# Nisan 2026 — Fix Detayları

### FIX-31: completionRate — Deadline Gelmemiş Görevler Hariç
**Sorun:** Gelecek deadline'lı görevler `totalAssignedTasks`'a giriyordu → completionRate düşüyordu → puan düşüyordu (417 → 385).

**Kök neden (LeaderboardWidget):** `t.status === "completed"` OR bypass'ı endDate kontrolünü atlıyordu.

**Kök neden (league page):** `monthEnd = "2026-04-30"` kullanılıyordu; aynı ay içindeki gelecek deadline'lar sayılıyordu.

**Kural:** `totalAssignedTasks` = yalnızca `endDate <= bugün` olan görevler. Aynı ay içinde olsa bile deadline gelmemişse sayılmaz.

**Düzeltmeler:**
- `src/app/components/dashboard/scoring/LeaderboardWidget.tsx` satır ~176
  - `(t.status === "completed" || ...)` bypass kaldırıldı
  - `(t.endDate ? t.endDate <= todayStr : false)` — endDate yoksa dahil etme
- `src/app/dashboard/league/page.tsx`
  - `todayStr` değişkeni eklendi
  - `monthlyAssigned = assignedInMonth(monthStart, todayStr, ...)`
  - Birikimli döngüde: `mEnd = month === currentMonthKey ? todayStr : mEndFull`

---

### FIX-32: Görev Oranı Görsel — `4/5 (+1)` Formatı
**Kural:** Deadline geçmiş görevler paydaya girer (puana dahil). Pending görevler paydaya dahil ama `(+N)` ile ayrıca gösterilir (puana dahil değil).

**Düzeltmeler:**
- `src/app/dashboard/league/page.tsx`
  - `RankedStudent` interface → `pendingTasksDisplay: number`
  - `monthlyPending`: `status !== completed` + `endDate > bugün` + `endDate <= monthEnd`
  - `totalPending`: `status !== completed` + `endDate > bugün`
  - `monthlyAssignedDisplay = (monthlyAssigned ?? monthlyCompleted) + monthlyPending`
  - UI: `4/5 (+1)` formatı, `(+N)` `text-text-tertiary` rengiyle

---

### FIX-33: "Ödevi Bitir" — Gelecek endDate Bugüne Çekilir
**Kural:** Öğretmen "Ödevi Bitir"e bastığında endDate gelecekteyse bugüne güncellenir.

**Düzeltme:**
- `src/app/components/dashboard/scoring/DesignParkour.tsx` — `handleComplete`
  - `if (!task.endDate || task.endDate > todayStr) updates.endDate = todayStr`

---

### FIX-34: "Ödevi İptal Et" — Doküman Tamamen Silindi
**Kural:** İptal = öğrenci puanları temizlenir + task `deleteDoc` ile silinir.

**Düzeltme:**
- `src/app/components/dashboard/scoring/DesignParkour.tsx` — `handleCancelTask`
  - `updateDoc({status:"archived"...})` → `deleteDoc(taskRef)`
  - `deleteDoc` import'a eklendi

---

### FIX-35: Grafik 1 → Grafik 2 Carry-Over — Grup Düzenlemede Eksikti
**Sorun:** Grup modülü GRAFIK_1→GRAFIK_2'ye değiştirildiğinde öğrencilere `g2StartXP` yazılmıyordu.

**Kural:** Grup düzenlemede `isModuleUpgrade` tespit edilince gruptaki tüm öğrencilere carry-over hesaplanır. Filtre: `gradedTasks[*].classId === eskiGrupKodu`. Double-application: `isCarryOverApplied: true`.

**Düzeltme:**
- `src/app/hooks/useManagement.ts` — grup düzenleme bloğu
  - `isModuleUpgrade = oldModule === "GRAFIK_1" && groupModule === "GRAFIK_2"`
  - `isCodeChange || isModuleUpgrade` → öğrenciler toplu batch güncellenir
  - Her öğrenciye: `g2StartXP`, `isCarryOverApplied: true`, `grafik1Code`
