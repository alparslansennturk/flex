name: Monthly Winner Preview

on:
  schedule:
    - cron: '0 6 28-31 * *'
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger monthly-winner-preview
        run: |
          curl -f -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            https://flex-one-iota.vercel.app/api/cron/monthly-winner-preview
